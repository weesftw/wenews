micronaut:
  server:
    port: 8081
rabbitmq:
  uri: ${RABBITMQ_URL}
mongo:
  database: wenews
ds:
  url: jdbc:mysql://localhost:3306/wenews?useTimezone=true&serverTimezone=UTC
newsapi:
  url: https://newsapi.org/v2/top-headlines?country=br&pageSize=20&apiKey=${NEWSAPI_KEY}
vault:
  path: ${VAULT_PATH:core/datasource}